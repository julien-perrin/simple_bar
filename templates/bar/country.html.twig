{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
    <h1>Beers origin {{ country.name }}</h1>
    {% if beers %}
        {% for beer in beers %}
            <li>{{ beer.name }}</li>
        {% endfor %}
    {% endif %}
{% endblock %}

